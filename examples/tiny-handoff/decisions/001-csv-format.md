# Decision 001 — use CSV

Status: superseded.

Initial decision: use comma-separated values because the Python standard library already understands CSV.

This was later superseded by Decision 002 after the tiny source fixture introduced task titles containing commas.
