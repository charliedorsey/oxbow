# Open questions

These are intentionally unresolved in the example:

- blank owner: empty string or JSON null?
- comment syntax: support lines beginning with `#` or reject them?

Do not close these merely because a later reader has a preference.
